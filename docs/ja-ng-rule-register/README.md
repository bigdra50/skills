<!-- apm:readme:begin (generated by scripts/gen-skill-readme.ts — do not edit this block) -->
# ja-ng-rule-register

> 日本語の言い回しへの指摘を、prh 辞書の検査ルールとして登録する手順。 候補パターンを既存コーパスへ当てて件数を数え、正当な用法が混ざるものは採用しない。 直し方を示すグッドパターンと、文ごと書き直させるメッセージをセットで書く。 Use when: 「この言い方をやめたい」「AI っぽい日本語を検出したい」「NG ワードを追加して」 「textlint のルールを足して」「prh 辞書に登録して」と依頼されたとき。 溜まった指摘をまとめて直すのは md-prose-cleanup、単一ファイルの推敲は japanese-tech-writing を使う。

## Install

### APM (recommended)

```bash
apm install bigdra50/skills/docs/ja-ng-rule-register
```

Or declare it in `apm.yml` for a reproducible setup:

```yaml
dependencies:
  apm:
    - bigdra50/skills/docs/ja-ng-rule-register
```

then run `apm install`.

### Any agentskills.io-compatible agent

This is an [Agent Skills](https://agentskills.io) skill — a portable
`SKILL.md` folder that works with Claude Code, Codex, opencode, Gemini CLI,
Cursor, and other [compatible agents](https://agentskills.io/clients).

With the [`skills`](https://github.com/vercel-labs/skills) CLI (installs into
every detected agent's skills directory):

```bash
npx skills add bigdra50/skills --skill ja-ng-rule-register
```

Or copy the folder in directly:

```bash
npx degit bigdra50/skills/docs/ja-ng-rule-register ~/.claude/skills/ja-ng-rule-register
```

See [SKILL.md](./SKILL.md) for what this skill does and when to use it.
<!-- apm:readme:end -->
