<!-- apm:readme:begin (generated by scripts/gen-skill-readme.ts — do not edit this block) -->
# review-performance

> Evaluate Unity-specific performance problems from a unilyze snapshot — GC allocation patterns, hot-path Unity API misuse, and Burst/DOTS readiness. Produces an impact-ranked findings table pointing at the offending method and line. Does NOT refactor, does NOT evaluate non-performance smells (that is review-metrics / review-safety), and does NOT profile at runtime — runtime profiling needs the Unity Editor and the unity-perf skill from unity-cli.

## Install

### APM (recommended)

```bash
apm install bigdra50/skills/unity/review-performance
```

Or declare it in `apm.yml` for a reproducible setup:

```yaml
dependencies:
  apm:
    - bigdra50/skills/unity/review-performance
```

then run `apm install`.

### Any agentskills.io-compatible agent

This is an [Agent Skills](https://agentskills.io) skill — a portable
`SKILL.md` folder that works with Claude Code, Codex, opencode, Gemini CLI,
Cursor, and other [compatible agents](https://agentskills.io/clients).

With the [`skills`](https://github.com/vercel-labs/skills) CLI (installs into
every detected agent's skills directory):

```bash
npx skills add bigdra50/skills --skill review-performance
```

Or copy the folder in directly:

```bash
npx degit bigdra50/skills/unity/review-performance ~/.claude/skills/review-performance
```

See [SKILL.md](./SKILL.md) for what this skill does and when to use it.
<!-- apm:readme:end -->
